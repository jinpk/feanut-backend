export enum NotificationConfigTypes {
  Pull = 'pull',
  Poll = 'poll',
}

export enum NotificationSettingTypes {
  Promotion = 'promotion',
  Service = 'service',
  Poll = 'poll',
  Pull = 'pull',
}
